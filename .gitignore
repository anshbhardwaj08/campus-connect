node_modules/
.env
# The server's log folders by name. A bare `logs/` also matched
# admin/src/pages/logs, so a source file went uncommitted and broke the deploy.
server/logs/
server/src/logs/
*.log
.DS_Store

# Machine-local editor/tool settings
.claude/settings.local.json

server/node_modules/
server/logs/
server/.env

client/node_modules/
client/dist/
client/.env

admin/node_modules/
admin/dist/
admin/.env
client/.ui-check/
admin/.ui-check/

# Data files for the throwaway MongoDB the test suite boots (tests/helpers/db.js)
server/.test-db/
