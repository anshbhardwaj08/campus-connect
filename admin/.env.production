# Build-time settings for the deployed admin panel (`vite build`). Served by
# the API server at /admin, so the API and socket are same-origin — see
# client/.env.production. No secrets here. Committed on purpose.
VITE_API_URL=/api/v1
VITE_SOCKET_URL=
